import { TestBed } from '@angular/core/testing';

import { NgxJsonresumeStreamlineService } from './ngx-jsonresume-streamline.service';

describe('NgxJsonresumeStreamlineService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: NgxJsonresumeStreamlineService = TestBed.get(NgxJsonresumeStreamlineService);
    expect(service).toBeTruthy();
  });
});
